pipeline{
    agent none
    stages {
      stage('build'){
          steps {
              echo "build this app"
          }
      }

      stage('test'){
          steps {
              echo "test this app"
          }
      }

      stage('deploy'){
          steps {
              echo "deploy this app"
          }
      }
    }
}
